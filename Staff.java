import java.io.IOException;
public class Staff extends Member
{   
    public Staff(String account,String name,String password,String identity)throws IOException{super(account,name,password,identity);}
}
