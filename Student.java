import java.io.IOException;
public class Student extends Member
{
    public Student(String account,String name,String password,String identity)throws IOException{super(account,name,password,identity);}
}
